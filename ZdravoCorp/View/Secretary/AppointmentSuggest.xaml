<Window x:Class="ZdravoCorp.View.Secretary.AppointmentSuggest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZdravoCorp.View.Secretary"
        mc:Ignorable="d"
        Title="AppointmentSuggest" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="SuggestionTable" ItemsSource="{Binding}" Margin="0,25,0,94" 
                              AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vreme" Binding="{Binding Time}" Width="*"/>
                <DataGridTextColumn Header="Soba" Binding="{Binding Room}" Width="*"/>

            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Otkazi zakazivanje" Margin="166,364,0,0" Height="30" Width="130" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <Button Content="Zakazi termin" Margin="370,364,0,0" Height="30" Width="130" VerticalAlignment="Top" HorizontalAlignment="Left"/>
    </Grid>
</Window>
